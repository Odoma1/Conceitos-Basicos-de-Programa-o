#include<stdio.h>
#include<math.h>

int main()
{
	int n, i, n1, n2;
	printf("\nInforme da tabuada:");
	scanf("%i", &n);
	printf("\nInforme quantos numeros serão multiplicados:");
	scanf("%i", &n2);
	for(i = 0; i<=n2 ; i++)
	{
	 n1 = n * i;
	 printf("Mulpitiplicado por %i: %i\n", i , n1);
		
	}
	
}
