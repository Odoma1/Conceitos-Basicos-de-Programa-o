#include <stdio.h>

main() {
    int numero, i;

    printf("Digite um número para ver sua tabuada: ");
    scanf("%i", &numero);

    printf("\nTabuada do %i:\n", numero);
    for (i = 1; i <= 10; i++) {
        printf("%i x %f = %i\n", numero, i, numero * i);
    }

    return 0;
}
