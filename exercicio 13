#include <stdio.h>

main() {
    float numero, i;

    printf("Digite um número para ver sua tabuada: ");
    scanf("%f", &numero);

    printf("\nTabuada do %f:\n", numero);
    for (i = 1; i <= 12; i++) {
        printf("%f x %f = %f\n", numero, i, numero * i);
    }

    return 0;
}
