#include <stdio.h>
#include <math.h>

int main()
{
	int n1;
	printf("informe o numero desejado:");
	scanf("%i", &n1);
	if(n1%2==0 || n1%5==0 || n1%10==0)
	{
		printf("\n %i E DIVISIVEL POR 2, 5, 10", n1);
	}
	else
	{
		printf("\n %i NAO E DIVISIVEL POR 2, 5, 10", n1);
	}
}
