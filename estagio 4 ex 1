#include<stdio.h>
#include<math.h>

 main()
{
	int n, soma, i;
	printf("informe a quantidade de numeros que serao lidos:");
	scanf("%i", &n);
	for(i=1; i<=n; i++)
	{  
		printf("%i\n", i);
  		soma= soma + i; 
	}    
	printf("A soma dos numeros é:%i", soma);
  }
